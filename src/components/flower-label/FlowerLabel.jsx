import "./FlowerLabel.scss";

const FlowerLabel = () => {
    return (
        <div className="flower-label">
            <strong className="flower-label__title">Fresh Flower</strong>
            <span className="flower-label__span">(25 items)</span>
        </div>
    );
};

export default FlowerLabel;
